# Virginia case study visuals (generated)

PNG and PDF exports from `scripts/visualize_virginia_memo.py` are written here:

- `png/` — raster outputs (`virginia_*.png`, optional `va*.png` if used)
- `vector/` — PDF companions

This keeps Virginia chart **results** next to the tracked case-study docs under `docs/states/virginia/`. Federal paper visuals remain under repository-root `visuals/`.

Regenerate after building inputs in `figures/` (see [../virginia_deep_dive.md](../virginia_deep_dive.md)).
